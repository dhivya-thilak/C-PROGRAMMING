#include <stdio.h>

int main()
{
    int n, i;
    int a = 0, b = 1, c;
    int sum = 0;

    printf("Enter number of terms: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        if(a % 2 == 0)
            sum = sum + a;

        c = a + b;
        a = b;
        b = c;
    }

    printf("Sum of even Fibonacci terms = %d", sum);

    return 0;
}
